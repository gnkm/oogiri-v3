{
  "name": "oogiri-v3",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "install": "if ! command -v curl >/dev/null || ! command -v jq >/dev/null; then sudo apt-get update && sudo apt-get install -y ca-certificates curl jq; fi; if [ -f pyproject.toml ]; then uv sync --all-groups; fi",
  "agentCanUpdateSnapshot": true
}
